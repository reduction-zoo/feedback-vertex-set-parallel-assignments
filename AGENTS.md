# Research campaign

Own only this campaign while working here. Follow `.agents/skills/research-session/SKILL.md`, `research/repository.md`, and `research/reduction.md`. Fixed question: `campaigns/feedback-vertex-set-parallel-assignments/question.md`; initial budget 20 rounds. All artifacts in English. Commit independent Prepare before candidate implementation. Retain failures, sequencing deviations and incremental commits. Use uv and a committed lockfile. Prove polynomial F/G, including every-valid-output recovery and large encoded integers.

Read shared experience in place at `/Users/xiweipan/Codes/autoresearch-gadgets/research/experience/entries/`; do not copy it. No sibling runtime dependencies. Preserve unrelated edits. No publication, board changes, upstream comments or merges.

User authorizes this same persistent Codex session to finish the assigned sequential queue. No DSH handoff required. Use registered `.codex/agents/research-reviewer.toml` with fresh context for independent review. Record actual model availability, isolation and measured usage only. Main researcher owns semantics, oracles and proof. Narrow mechanical delegation only when useful; no full campaign agents.

After completion or justified stop, continue hamiltonian-path-consecutive-blocks and the remaining user-assigned queue in order. Never select outside that queue. Skip existing destinations unless created for this queue by this session, and report skips.
